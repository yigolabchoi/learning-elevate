# Learning Elevate – 프로젝트 구조

## 📋 문서 정보

- **작성일**: 2025-11-17
- **버전**: 0.1.0
- **상태**: 초안 (개발 스펙 대기 중)

---

## 목차

1. [전체 구조 개요](#1-전체-구조-개요)
2. [프론트엔드 구조](#2-프론트엔드-구조)
3. [백엔드 구조](#3-백엔드-구조)
4. [공유 모듈](#4-공유-모듈)
5. [설정 파일](#5-설정-파일)

---

## 1. 전체 구조 개요

**개발 스펙에 따라 업데이트 예정**

### 1-1. 모노레포 구조 (예시)

```
learning-elevate/
├── apps/
│   ├── web/                 # 프론트엔드 (Next.js/React)
│   ├── api/                 # 백엔드 API
│   └── admin/               # 관리자 대시보드 (선택)
├── packages/
│   ├── ui/                  # 공유 UI 컴포넌트
│   ├── types/               # 공유 TypeScript 타입
│   ├── utils/               # 공유 유틸리티
│   └── config/              # 공유 설정
├── docs/                    # 문서
├── scripts/                 # 빌드/배포 스크립트
├── .github/                 # GitHub Actions
├── docker-compose.yml
├── package.json
├── turbo.json               # Turborepo 설정 (선택)
└── README.md
```

### 1-2. 단일 저장소 구조 (예시)

```
learning-elevate/
├── frontend/                # 프론트엔드
├── backend/                 # 백엔드
├── shared/                  # 공유 코드
├── docs/                    # 문서
├── docker-compose.yml
└── README.md
```

---

## 2. 프론트엔드 구조

**개발 스펙에 따라 업데이트 예정**

### 2-1. Next.js App Router 구조 (예시)

```
apps/web/
├── src/
│   ├── app/                           # App Router
│   │   ├── (auth)/                    # 인증 그룹
│   │   │   ├── login/
│   │   │   ├── signup/
│   │   │   └── layout.tsx
│   │   ├── (dashboard)/               # 대시보드 그룹
│   │   │   ├── teacher/
│   │   │   │   ├── dashboard/
│   │   │   │   ├── curriculum/
│   │   │   │   ├── students/
│   │   │   │   └── review/
│   │   │   ├── student/
│   │   │   │   ├── dashboard/
│   │   │   │   ├── solve/
│   │   │   │   └── history/
│   │   │   ├── parent/
│   │   │   │   ├── dashboard/
│   │   │   │   └── report/
│   │   │   └── layout.tsx
│   │   ├── api/                       # API Routes
│   │   │   ├── auth/
│   │   │   ├── curriculum/
│   │   │   └── questions/
│   │   ├── layout.tsx
│   │   └── page.tsx
│   ├── components/                    # 컴포넌트
│   │   ├── ui/                        # 기본 UI 컴포넌트
│   │   │   ├── button.tsx
│   │   │   ├── input.tsx
│   │   │   ├── card.tsx
│   │   │   └── ...
│   │   ├── features/                  # 기능별 컴포넌트
│   │   │   ├── curriculum/
│   │   │   ├── question/
│   │   │   ├── student/
│   │   │   └── ...
│   │   └── layout/                    # 레이아웃 컴포넌트
│   │       ├── header.tsx
│   │       ├── sidebar.tsx
│   │       └── footer.tsx
│   ├── lib/                           # 라이브러리 및 유틸리티
│   │   ├── api/                       # API 클라이언트
│   │   ├── hooks/                     # 커스텀 훅
│   │   ├── utils/                     # 유틸리티 함수
│   │   └── constants/                 # 상수
│   ├── store/                         # 상태 관리
│   │   ├── slices/                    # Redux 슬라이스 (Redux 사용 시)
│   │   ├── atoms/                     # Recoil 아톰 (Recoil 사용 시)
│   │   └── stores/                    # Zustand 스토어 (Zustand 사용 시)
│   ├── types/                         # TypeScript 타입
│   │   ├── api.ts
│   │   ├── models.ts
│   │   └── ...
│   └── styles/                        # 스타일
│       ├── globals.css
│       └── ...
├── public/                            # 정적 파일
│   ├── images/
│   ├── icons/
│   └── ...
├── tests/                             # 테스트
│   ├── unit/
│   ├── integration/
│   └── e2e/
├── .env.local
├── next.config.js
├── tailwind.config.js
├── tsconfig.json
└── package.json
```

### 2-2. 주요 디렉토리 설명

#### `app/`
- Next.js App Router 기반 라우팅
- 파일 시스템 기반 라우팅
- 레이아웃 및 페이지 컴포넌트

#### `components/`
- **ui/**: 재사용 가능한 기본 UI 컴포넌트 (Button, Input 등)
- **features/**: 기능별 복합 컴포넌트
- **layout/**: 레이아웃 관련 컴포넌트

#### `lib/`
- **api/**: API 호출 함수 및 클라이언트
- **hooks/**: 커스텀 React 훅
- **utils/**: 유틸리티 함수
- **constants/**: 상수 정의

#### `store/`
- 전역 상태 관리
- Redux/Recoil/Zustand 등

#### `types/`
- TypeScript 타입 정의
- API 응답 타입
- 도메인 모델 타입

---

## 3. 백엔드 구조

**개발 스펙에 따라 업데이트 예정**

### 3-1. Express/NestJS 구조 (예시)

```
apps/api/
├── src/
│   ├── modules/                       # 기능 모듈
│   │   ├── auth/
│   │   │   ├── auth.controller.ts
│   │   │   ├── auth.service.ts
│   │   │   ├── auth.module.ts
│   │   │   └── dto/
│   │   ├── curriculum/
│   │   │   ├── curriculum.controller.ts
│   │   │   ├── curriculum.service.ts
│   │   │   ├── curriculum.module.ts
│   │   │   └── dto/
│   │   ├── questions/
│   │   │   ├── questions.controller.ts
│   │   │   ├── questions.service.ts
│   │   │   ├── questions.module.ts
│   │   │   └── dto/
│   │   ├── students/
│   │   ├── teachers/
│   │   ├── parents/
│   │   └── ai/                        # AI 통합
│   │       ├── ai.service.ts
│   │       ├── prompts/
│   │       └── ...
│   ├── common/                        # 공통 모듈
│   │   ├── decorators/
│   │   ├── filters/
│   │   ├── guards/
│   │   ├── interceptors/
│   │   ├── pipes/
│   │   └── middleware/
│   ├── config/                        # 설정
│   │   ├── database.config.ts
│   │   ├── jwt.config.ts
│   │   └── ...
│   ├── database/                      # 데이터베이스
│   │   ├── migrations/
│   │   ├── seeds/
│   │   └── entities/                  # TypeORM 엔티티
│   ├── utils/                         # 유틸리티
│   ├── types/                         # 타입 정의
│   ├── app.module.ts
│   └── main.ts
├── test/                              # 테스트
│   ├── unit/
│   ├── integration/
│   └── e2e/
├── prisma/                            # Prisma (사용 시)
│   ├── schema.prisma
│   ├── migrations/
│   └── seed.ts
├── .env
├── .env.example
├── tsconfig.json
└── package.json
```

### 3-2. 주요 디렉토리 설명

#### `modules/`
- 기능별 모듈 구조
- Controller, Service, Module 패턴
- DTO (Data Transfer Object)

#### `common/`
- 공통 데코레이터, 필터, 가드 등
- 재사용 가능한 미들웨어

#### `config/`
- 환경별 설정
- 데이터베이스, JWT, AI API 등

#### `database/`
- 마이그레이션
- 시드 데이터
- 엔티티 정의

---

## 4. 공유 모듈

**모노레포 사용 시**

### 4-1. UI 패키지

```
packages/ui/
├── src/
│   ├── components/
│   │   ├── Button/
│   │   ├── Input/
│   │   └── ...
│   ├── styles/
│   └── index.ts
├── package.json
└── tsconfig.json
```

### 4-2. 타입 패키지

```
packages/types/
├── src/
│   ├── api/
│   ├── models/
│   └── index.ts
├── package.json
└── tsconfig.json
```

### 4-3. 유틸리티 패키지

```
packages/utils/
├── src/
│   ├── validation/
│   ├── formatting/
│   └── index.ts
├── package.json
└── tsconfig.json
```

---

## 5. 설정 파일

### 5-1. 루트 설정 파일

```
learning-elevate/
├── .gitignore
├── .prettierrc
├── .eslintrc.js
├── tsconfig.json
├── docker-compose.yml
├── turbo.json              # Turborepo (선택)
└── package.json
```

### 5-2. 환경 변수 파일

```
.env.example                # 예시 환경 변수
.env.local                  # 로컬 개발 환경
.env.development            # 개발 환경
.env.staging                # 스테이징 환경
.env.production             # 프로덕션 환경
```

---

## 6. 네이밍 컨벤션

### 6-1. 파일명

- **컴포넌트**: PascalCase (예: `Button.tsx`, `UserProfile.tsx`)
- **유틸리티**: camelCase (예: `formatDate.ts`, `validateEmail.ts`)
- **상수**: UPPER_SNAKE_CASE (예: `API_ENDPOINTS.ts`)
- **타입**: PascalCase (예: `User.ts`, `ApiResponse.ts`)

### 6-2. 디렉토리명

- kebab-case 또는 camelCase 일관성 있게 사용
- 복수형 사용 (예: `components/`, `utils/`, `types/`)

---

## 7. 관련 문서

- [개발 환경 설정](./setup-guide.md)
- [코딩 컨벤션](./coding-conventions.md)
- [데이터베이스 스키마](./database-schema.md)
- [API 명세서](../api/api-specification.md)

---

## 변경 이력

| 날짜 | 버전 | 변경 내용 | 작성자 |
|------|------|----------|--------|
| 2025-11-17 | 0.1.0 | 초안 작성 (개발 스펙 대기 중) | Dev Team |

---

**문서 상태**: 🟡 초안 (Draft) - 개발 스펙 정보 필요  
**다음 업데이트**: 개발 스펙 확정 후

